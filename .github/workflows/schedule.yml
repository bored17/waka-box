name: Update Gist

on:
  push:
    branch: master

  schedule:
    - cron: "0 */3 * * *"

jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Install
        run: npm ci
      - name: Update gist
        run: node ./index.js  --unhandled-rejections=strict
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: d0c9701d8ad09d8f1f2297fe53d421b5
          WAKATIME_API_KEY: ${{ b36933dd-a2ed-49b3-bd6c-7d2c47711d3d }}
